import { StyleSheet, Text, View } from "react-native";
import React from "react";

const HorizontalBar = ({ className }: { className: string }) => {
  return <View className={className} />;
};

export default HorizontalBar;
